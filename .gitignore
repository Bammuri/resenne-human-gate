__pycache__/

# Node dev-only test deps (fast-check); package.json/package-lock.json are committed
node_modules/

# Python dev-only test venv (hypothesis); PEP 668 externally-managed base env
.venv/

# Runtime state written by the Claude hooks (not source)
.claude/binddeck-state.json
.claude/binddeck-state.json.tmp
.hypothesis/

# WSL/Windows metadata (NTFS alternate data streams materialized as files in WSL)
*:Zone.Identifier
