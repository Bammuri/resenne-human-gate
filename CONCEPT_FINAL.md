# Re:senne HUMAN GATE — 컨셉 확정본 (2026-09-08 갱신)

> 확인된 하드웨어·구현 현실에 맞춰 컨셉을 **최종 확정**한 문서. **약속할 것과 약속하지 않을 것**을 못박는다.
> 상세 실행계획은 [`HACKATHON_EXECUTION_PLAN.md`](./HACKATHON_EXECUTION_PLAN.md).
> 근거: gpt-6-astra(reasoning=max) 2차 판정 + 팀 확인 사실. 마감 **9/9(수) 17:00**.

---

## 0. 확정 컨셉 3줄

1. **HUMAN GATE — 물리 버튼으로 실제 Claude Code의 "대기 중인 실행"을 승인·거절한다.**
2. **리센느를 단일 히어로로 낸다.** 코어는 라이선스를 갖춰 오픈소스로 제출하고, **AI-DLC 8키 컨트롤러(`demo/arduino-simulator/`)도 코드로 구현·커밋**해 저장소에 포함한다(히어로는 리센느 하나) — 코드는 소프트웨어 테스트됨(가짜 CLI), 실제 AI·실기기는 사람 확인.
3. **사진 ✓키캡·✕버튼·실제 실행 결과를 한 컷에** 담고, 현장 실패 시 **검증된 B 원본 영상**을 재생한다.

---

## 0.5 포지셔닝 & 캐치프레이즈 (전체 관통)

> 이 절은 §0의 **검증 약속을 바꾸지 않는다.** 카테고리(범위)를 넓히고 Claude Code를 그 **검증된 예시**로 자리매김할 뿐이다.

**재정의 = 비전을 당당히 선언한다. 지금 못 한다고 미래를 축소하지 않는다.**

- 매크로패드·스트림덱은 버튼으로 **실행을 촉발**한다. 리센느는 그 카테고리를 **AI 실행의 승인·거절 게이트**로 **재정의한다.** 이 전복은 지금 실물(버튼 2개 + 실제 Claude Code 승인·거절)로 **이미 성립**했고, **8키·로터리·다중 하네스(Codex·Claude) 확장도 `demo/arduino-simulator/`에 코드로 구현·커밋**했다(코드=소프트웨어 테스트됨/가짜 CLI, 실제 AI·실기기=사람 확인). → **"AI custom stream deck을 재정의했다"는 정당한 선언이다.** 카테고리(버튼의 의미)를 실제로 바꿨으니 현재·완료형으로 말해도 거짓이 아니다.
- 🚫 **유일한 선 = 두 층위를 안 섞기.** ① `demo/arduino-simulator/` **코드는 구현·커밋·소프트웨어 테스트(116개)** 됐다 — 단 테스트는 **가짜 CLI 프로세스**를 쓴다. ② 실제 Codex·Claude 응답·물리 보드·물리 키를 통한 AI-DLC 단계 제어는 **팀이 실기기로 확인(사람 검증·시연)**했으며 **자동 테스트가 증명하지 않는다.** 이 둘을 뭉뚱그려 **"가짜 CLI 테스트로 실제 AI 제어까지 증명됐다"** 고 말하지 않는다. **미래형·확장 비전(다중 하네스 일반화 등)은 얼마든지 크게 말한다** — 기대는 당연하고 환영한다. 다만 **생산성 향상·모든 에이전트 호환은 여전히 현재 성과로 말하지 않는다.**
- 걸그룹 에너지: **리센느가 무대의 문법을 다시 썼듯, HUMAN GATE는 AI 컨트롤 서피스의 문법을 다시 쓴다 — "실행 촉발"에서 "실행 승인"으로, 그리고 그 너머로.** ("데뷔" 톤도 함께 활용)

**범위 = AI 하네스를 포괄, Claude Code는 검증된 예시** (팀 정정 반영):

- **카테고리·설계 차원(참):** "사람 승인이 필요한 순간"이 있는 **AI 하네스(에이전트) 범용** 게이트. 프론트엔드·아두이노에 하네스 전용 로직 없음 = **공통 프로토콜 + 어댑터**(참고 기획서 agent-agnostic 원칙).
- **검증 차원(정직 경계):** 지금 **E2E 검증된 예시는 Claude Code 하나.** 실제 Claude Code의 대기 중 실행을 승인·거절함(경로 B, 팀 실기기).
- **다른 하네스 = 어댑터로 확장하는 "방향"** 이지 "동작 검증됨"이 아니다. **"모든 에이전트 호환"을 현재 성과로 말하지 않는다.**

### 확정 캐치프레이즈 세트

- **메인:** **버튼의 재정의. 명령에서 승인·거절로.**
  *Redefining the button. From commands to consent.*
- **서브:** AI 하네스의 **대기 중 실행**을 사람이 승인·거절하는, 커스텀 가능한 오픈소스 **HUMAN GATE**. 지금 검증된 예시 = **Claude Code.**
  *A customizable, open-source HUMAN GATE for AI harnesses — two physical buttons that relay human approval or rejection to a pending action. Verified example: Claude Code.*
- **오프닝(발표 1문장):** 오늘 **데뷔**하는 Re:senne HUMAN GATE는, AI 하네스의 실행 요청에 사람이 **두 버튼**으로 답하는 새로운 역할을 선보입니다 — 그 첫 실증이 **Claude Code**입니다.
- **썸네일 대카피:** **실행 앞에, 사람.** *(Before it runs, a human.)*
- **포지셔닝 1문장:** Re:senne HUMAN GATE는 리센느를 **첫 테마 프로파일**로 구현한 커스텀 가능한 오픈소스 물리 컨트롤러로, 버튼의 역할을 '실행 호출'에서 '**AI 하네스의 대기 중 실행에 대한 사람의 승인·거절 게이트**'로 재정의한다. 지금 검증된 예시는 **Claude Code**이며, 다른 하네스는 **공통 프로토콜·어댑터**로 확장한다.

**증명 경계(심사·영상에서 넘지 말 것):** 증명된 문장 = **"사람의 물리 입력이 실제 Claude Code의 대기 중 실행에 승인·거절 결정을 전달했다"** + **"`demo/arduino-simulator/` 8키 컨트롤러가 물리 키로 실제 Codex·Claude와 AI-DLC 단계 제어를 구동한다(코드 커밋·소프트웨어 테스트/가짜 CLI, 실제 AI·실기기는 사람 확인)."** 그 이상(모든 에이전트 호환 · AI 전반 통제 · 생산성 향상, 그리고 **가짜 CLI 테스트가 실제 AI 제어를 증명한다는 주장**)은 현재 성과로 말하지 않는다.

---

## 1. 진실 경계 — 공개 약속은 (a) "진짜 실행 승인"으로 올린다

- **경로 B가 팀 실기기 E2E로 검증됨:** 물리 버튼 → 브리지 → 기존 Python PTY 승인 처리 → **진짜 Claude Code 실행**까지 왕복. 승인하면 실행, 거절하면 해당 요청 미실행.
- 따라서 공개 문구 = **"물리 버튼으로 실제 Claude Code의 대기 중인 작업 실행을 승인한다."** (신호 전달까지만으로 낮출 이유 없음)
- **필수 증거 1개 = 무편집 영상:** `승인 대기 → ✓ 누름 → 요청한 파일의 실제 변경`을 끊김 없이. 거절 주장까지 담으려면 같은 영상에 `새 요청 → ✕ → 해당 요청 취소·변경 미발생`.
- **검증 주체 = "팀 실기기 E2E"로 명시.** AI(개발환경)의 재검증을 채택 조건으로 다시 걸지 않는다. 대신 **검증 담당자가 영상에 대응하는 제출 커밋·브리지·펌웨어·Claude 버전·실행법을 기록**한다.
- **B-검증 게이트 = 제출 repo에 통합된 `hooks/hook_bridge.py` + `server.py`의 `/api/approval*`(PreToolUse allow/deny).** 이 allow/deny 게이트(승인 브로커 + `hook_bridge.py`)는 제출 repo에 **코드 통합·단위검증·소프트웨어 E2E 완료**(`server.py`의 `/api/approval*` + `hooks/hook_bridge.py` + `tests/test_approval.py`의 브로커·서브프로세스 E2E 테스트 + 브라우저 승인 패널 `/gate.html`·`gate.js` + 실서버 curl 스모크, 2026-09-09) — 브라우저(ui 토큰)에서 **사람이 대기 중 도구 실행에 allow/deny를 제출할 수 있음**. **실행 중인 진짜 `claude`가 그 결정을 존중해 실제 실행을 allow/deny 하는 라이브 링크는 미검증(사람/라이브 데모 몫)**, 별도 예시 settings로 옵트인. 설계 = `aidlc-docs/construction/approval-gate/design.md`. → 아래 §7.

---

## 2. multi-type = "커스텀 가능한 오픈소스" (채택하되 제품군 나열 금지)

- **원칙:** 하나의 승인 경험을 재사용하는 **공통 코어 + 프로파일** 구조는 가점 근거. 단 **미구현 타입 나열은 DOCKPAD 실수의 반복** — 하지 않는다.
- **분류 축이 다르다:** **리센느 = 팀 테마**(사진 키캡·팀 맞춤), **AI-DLC = 작업 흐름**. 동급 제품 2개처럼 제시하지 않는다.
- **메인 문구:** "HUMAN GATE — Claude 실행 승인을 손으로. **리센느는 첫 커스텀 구현.**"
- **첫 화면/썸네일:** 리센느 실물 **하나** + B 실행 증거. (타입 도식 넣지 않음)
- **점수 주장:** 창의성 = **팀 맞춤 물리 인터랙션**. 유지보수 = **실제 공통 코어·설정 분리**. → **타입 "개수" 자체는 가점 근거에서 제외.**

### 근거가 되는 두 실제 트랙 (베이퍼웨어 아님)
| 트랙 | 위치 | 메커니즘 | 상태 |
|---|---|---|---|
| 범용/리센느 커스텀 | 제출 repo `server.py`·`hooks/hook_bridge.py`·`gate.html`/`gate.js` | PreToolUse hook로 Claude 도구 실행을 정지 + 브라우저/버튼 승인 패널 + 결정 감사 로그(`/api/approval/log`) | **코드 통합+단위검증+SW E2E**(subprocess·curl) · 라이브 `claude` 존중 = 사람 몫 (완전 E2E 근거 = PTY 경로) |
| AI-DLC 8키 컨트롤러 | `demo/arduino-simulator/`(펌웨어 `firmware/simulator_r4/`) | 버튼 8개→PTY 실키 + AI-DLC 5단계 7키 매핑 + WiFi HTTP 직결(`board_wifi.py`) | **코드 구현·커밋·SW 테스트 116개**(가짜 CLI) · 실제 AI·실기기 = 사람 확인(§3) |

---

## 3. AI-DLC type = `demo/arduino-simulator/`에 구현·커밋 (두 층위로 정직하게)

AI-DLC 8키 컨트롤러는 더 이상 로드맵이 아니라 **코드로 구현·커밋**됐다. 단, 표현은 두 층위로 못박는다:

| 층위 | 상태 | 근거 |
|---|---|---|
| ① 코드 = 구현·커밋·소프트웨어 테스트 | ✅ | `demo/arduino-simulator/`: 버튼 8개→PTY 실키(`terminal.py`), AI-DLC 5단계 7키 매핑(`workflow.js`), 자율성 0–5(AUTONOMY), `aidlc-docs/01~05-*.md` 기록(`workflow_documents.py`). 테스트 116개(Py 76 + JS 40). **단 테스트는 가짜 CLI 프로세스.** |
| ② 실제 AI·실기기 = 사람 확인 | ✅ (사람 검증·시연) | 실제 Codex·Claude가 물리 키에 반응하고 AI-DLC 단계 제어까지 동작함을 **팀이 실기기로 확인.** 자동 테스트가 증명하지 않음 — 무편집 시연 영상이 근거. |

- **넘지 말 선:** ①(코드 커밋·소프트웨어 테스트)을 ②(실제 AI 제어)와 **뭉뚱그리지 않는다.** "가짜 CLI 테스트 116개 통과"가 곧 "실제 Claude 제어 검증"을 뜻하지 않는다.
- 이 repo `CLAUDE.md`의 **"Wait for Explicit Approval"** 규칙과 `demo/arduino-simulator/`의 단계별 human-gate는 **실제 사용 장면**이다.
- **여전히 보류:** "생산성 향상" 같은 측정 전 주장, "모든 에이전트 호환"을 현재 성과로 말하는 표현.

---

## 4. 완성도 3티어 (B 반영 · 데모 기준)

| 티어 | 확정 범위 | 화면 |
|---|---|---|
| **최소 (무조건 확보)** | 리센느 실물 + **실제 Claude 승인 E2E 무편집 원본 영상** + 영상과 일치하는 제출 코드. 거절은 해당 요청 미실행까지 증명. | 버튼+실제 Claude+실제 변경 한 화면 |
| **목표 (되면 최고)** | 현장 **라이브** 승인·거절, 승인 내용·결과 화면 표시. **재시작 포함 승인·거절 각 3회 성공** 기록. | 라이브 분할 화면 |
| **포기선 (9/9 12:00 기능 동결)** | 불안정한 라이브 → **사전 촬영 B 영상**으로 전환. **AI-DLC 신규 구현·OLED 고도화·추가 HW 제외.** | 사전 촬영본(라벨 표기) |

- 검증에 쓴 **2.4GHz 네트워크 구성 그대로 준비** + B 영상 노트북에 저장.
- 현장 연결·응답 실패가 **10초 지속되면 "사전 촬영" 표시하고 재생.** 수신 로그·모의 Claude를 실제 B 증거로 대체 금지.
- **multi-type 설명 = 히어로 성공 직후 8초.** 리센느 실물 계속 보여주며 **"리센느 구현 완료 / AI-DLC 8키 컨트롤러도 구현·커밋(코드 테스트, 실기기는 사람 확인)"**만. 타입 전환 데모 없음.

---

## 5. 기억시킬 단 한 컷 (구도 유지 · 증거 상향)

> **손가락이 리센느 사진 ✓키캡을 누른다. 옆에 ✕버튼. 뒤 노트북에는 실제 Claude 승인 요청과 그 요청으로 바뀐 파일·페이지가 보인다.**

- 이제 기억시킬 결과는 신호 수신이 아니라 **실제 실행.** 브리지 로그는 보조로 내린다.
- OLED는 **필수 구성 제외** 유지. multi-type 도식은 이 컷에 넣지 않는다.
- 필수 준비물: 사진 키캡 · 큼직한 **`✓ 승인` / `✕ 거절`** 라벨 · 낮은 받침 · 손이 사진 안 가리는 **45° 사선**.

---

## 6. 다시 자기기만할 위험 — 아래 주장을 차단한다

- ❌ **"팀 PC에서 성공했으니 제출물도 완성됐다"** — 현재 체크아웃 README/컨셉 문서는 여전히 B 미통합으로 적혀 있다. **검증된 브리지 코드·실행법·문서를 제출 커밋에 맞춘다**(§7).
- ❌ **"한 번 성공했으니 안전하다"** — 중복·지연·대기 없는 입력이 **다음 요청을 승인하지 않는지** 별도 확인. ✓가 "항상 허용"을 확정하는 경우도 점검.
- ❌ **"✕는 AI 전체를 멈춘다"** — 공개 범위 = **해당 대기 요청의 거절**. 이미 실행된 작업 취소·전체 통제로 확대 금지.
- ✅ **라이선스 명시됨** — 루트 **`LICENSE`(Apache-2.0) + `NOTICE` 추가**(2026-09-08, gpt-6-astra 자문; **팀 최종 sign-off 대기**). 제3자 vendor(xterm.js·addon-fit, MIT)는 각 LICENSE 동봉, 물리 하드웨어 설계·팀 사진 키캡 권리는 미보장. 이전의 "소스 공개 PoC" 표현은 대체됨. (미해결 라이선스 코드는 배포 대상 제외 원칙 유지.)
- ❌ **"물리화했으니 오승인 감소·생산성 향상"** — 측정 전 주장 금지. 이번에 증명하는 것은 **사람의 물리 입력이 실제 실행 결정을 전달한다**는 것뿐.

---

## 7. 확정 후 남은 작업 (게이트됨 — 별도 지시로 착수)

1. **B-검증 PoC 임포트:** ✅ allow/deny 게이트(승인 브로커 + `hooks/hook_bridge.py`)를 제출 repo로 **코드 통합·단위검증·소프트웨어 E2E 완료**(2026-09-09, 옵트인 예시 settings, `terminal.py`·프로젝트 `.claude/settings.json` 불변). 진짜 `hook_bridge.py` 프로세스 ↔ 브로커 ↔ 브라우저(ui 토큰) resolver 체인을 서브프로세스 테스트 3개 + 실서버 curl 스모크로 검증, **브라우저 승인 패널 `/gate.html`**(별도 페이지, PTY 데모 불변) 추가. **실행 중인 진짜 `claude`가 결정을 존중해 실제 실행을 allow/deny 하는 라이브 링크는 미검증(사람/라이브 데모 몫)**, PTY 키응답이 유일 완전 E2E 경로. Node Bridge↔Python(PTY) 어댑터 실행법 통합은 여전히 남은 작업(§2). **결정 감사 로그 증분(2026-09-09):** 처리된 결정만 `.claude/approval-log.jsonl`(0600·웹루트 밖·gitignore)에 `tool_name·input_hash·decision·role·시각`만 기록(summary·tool_input·토큰 제외) + 최근 결정 뷰(`GET /api/approval/log`, ui/device만). `role`=자격증명 역할일 뿐 신원·물리버튼·실행결과 증명 아님, 기록 실패 시 결정 미전달(fail-safe ask). README/PLAN/컨셉 문서를 이 시제로 정합화 완료. 설계 = `aidlc-docs/construction/approval-gate/design.md`.
2. **라이선스 명시:** ✅ 루트 `LICENSE`(Apache-2.0) + `NOTICE` 추가(2026-09-08). **팀 최종 sign-off 대기.** 제3자 vendor 라이선스 동봉 확인, 미해결 라이선스 코드 배포 제외 원칙 유지.
3. **재현·안전 확인:** 중복/지연/대기없음/연결끊김 + 거절=미실행 3회.
4. **미디어:** 무편집 B 영상(오늘 밤 확보) + 히어로 컷 + 증거 스샷 2.
5. **A4/A5 5V 풀업:** 팀 관찰 vs 공식 회로도(미실장 표기) 충돌 → 실기기 재확인(OLED 필수 아니므로 저위험).

---

*확정 근거: gpt-6-astra(reasoning=max) 1·2차 판정 + 캐치프레이즈 판정(§0.5) + 팀 확인 사실. 원문 = 세션 산출물 `astra_concept_final_out.txt`, `astra_concept_final2_out.txt`, `astra_catchphrase_out.txt`.*
