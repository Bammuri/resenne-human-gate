# Re:senne HUMAN GATE — 제4회 디디톤 전체 실행계획서

> **한 줄 컨셉:** AI의 *실행 승인권*을 사람 손에 쥐여주는 실물 버튼, 리센느.
> **팔 장면 하나:** "진짜 Claude가 기다린다. 사람이 누른다. 그제야 코드가 바뀐다."
> **카피(권위본 = [`CONCEPT_FINAL.md` §0.5](./CONCEPT_FINAL.md)):** "버튼의 재정의. 명령에서 승인·거절로." · *Redefining the button. From commands to consent.* · 썸네일 = "실행 앞에, 사람."

> 🔒 **컨셉 확정 갱신 (2026-09-08) — 진실 경계는 [`CONCEPT_FINAL.md`](./CONCEPT_FINAL.md)가 우선한다.**
> 확인된 현실: **경로 B(버튼→브리지→Python PTY→진짜 Claude 실행)는 버튼→브리지 왕복까지 실기기 확인 — 완전 E2E 검증 경로는 PTY 키응답(제출 저장소에서 바로 재현).** B-검증 PoC의 allow/deny 게이트(승인 브로커 + `hooks/hook_bridge.py`)는 이제 제출 repo에 **코드 통합·단위검증·소프트웨어 E2E 완료**(브로커·서브프로세스 E2E 테스트 + 브라우저 승인 패널 `/gate.html` + 실서버 curl 스모크) — 브라우저(ui 토큰)에서 **사람이 대기 중 도구 실행에 allow/deny를 제출할 수 있음**. **실행 중인 진짜 claude가 그 결정을 존중해 실제 실행을 allow/deny 하는 라이브 링크는 미검증(사람 몫)** — 별도 예시 settings로 옵트인 활성화(2026-09-09, astra 배선범위 A·패널 C). PTY 키응답이 제출 유일 **완전** E2E 검증 경로.
> 따라서 **출품 공개 약속 = "물리 버튼으로 실제 Claude Code의 대기 중인 실행을 승인·거절한다" (a)**. 필수 증거 = 무편집 B 영상. 현장 라이브 실패 시 사전 촬영본(라벨 표기)로 전환, **9/9 12:00 기능 동결.**
> **multi-type = "커스텀 가능한 오픈소스 코어 + 프로파일"** — 리센느(팀 테마)를 **단일 히어로**로, **AI-DLC 8키 컨트롤러(`demo/arduino-simulator/`)는 코드 구현·커밋·소프트웨어 테스트**(116개, 가짜 CLI; 실제 AI·실기기는 사람 확인). 타입 개수는 가점 근거 아님.
> OLED는 **필수 구성 제외**(노트북 화면이 내용 표시). **라이선스 = Apache-2.0** — 루트 `LICENSE`·`NOTICE` 추가 완료(2026-09-08, gpt-6-astra 자문; **팀 최종 sign-off 대기**). 이전의 "소스 공개 PoC" 표현은 대체됨.
> **전체 관통 포지셔닝·캐치프레이즈 = [`CONCEPT_FINAL.md` §0.5](./CONCEPT_FINAL.md).** 메인 = **"버튼의 재정의. 명령에서 승인·거절로."** 범위 = **AI 하네스 포괄, Claude Code는 검증된 예시**(다른 하네스는 어댑터 확장 방향, "동작 검증됨" 아님). 제목 블록·§4·§6의 카피도 §0.5로 **통일**했다(옛 카피 `AI가 기다리는 버튼`·`코딩은 Claude가…`·`AI keeps coding…`은 미사용).
> **AI 심사 rubric 정합 (2026-09-08 갱신):** 문제 정의를 대상·빈도·현행으로 구체화 → **§0**(표) + **§0.6**(제출 README `## 문제`/`## 해결 방법` 확정 블록). rubric 6항목 대비 근거·정직 가드·닫아야 할 갭 = **§0.7 매트릭스**. rubric 원문 = `SCORE_WEIGHTS` 기반 100점(AI-DLC 25/문제정의 20/창의성 15/완성도 15/사용성 15/유지보수·보안 10), 최종 = AI 40% + 인간투표 60%.

- 대회: 제4회 디디톤 (4th DDthon) · 슬로건 "Humans set the direction. AI brings the speed"
- 팀: 리센느(Re:senne) · 강점 = **SW + HW 결합** (손으로 눌러 보여주는 실물 디바이스)
- 마감: **9/9(수) 17:00 제출** → 17:00~17:30 상호투표 → 시상. 가용시간 ≈ 하루.
- 채점: AI-DLC 25 / 문제정의 20 / 창의성 15 / 완성도 15 / 사용성 15 / 유지보수·보안 10.
  **최종 = AI심사 40% + 상호투표 60%** (심사위원은 AI점수 ±5만 조정).
- 기획 모델: gpt-6-astra(fast) 최종안 + gpt-5.6-luna 보완 + 팀 정정 반영.

---

## 0. 전략 프레이밍 (왜 이 컨셉인가)

상호투표는 **계정당 서로 다른 5팀**을 고른다. 한 사람의 압도적 1등보다 **"모든 사람의 기억나는 5팀"에 드는 것**이 이긴다. 우리의 무기는 기능 개수가 아니라 **세 가지가 한 프레임에 동시에 보이는 것**이다:

1. **실제 Claude 터미널** — 작동하는 소프트웨어라는 증거
2. **사람의 손 + 실물 버튼** — 순수 SW 팀과 구별되는 이미지
3. **누른 전후의 실제 변화** — 설명 없이 이해되는 효용

**문제 정의 (AI rubric "문제 정의 및 해결" 20점 — 대상·빈도·현행을 구체화):**
한 줄로: **작업 지시와 실행 승인을 같은 키보드로 처리한다. 리센느는 실행 승인·거절을 타이핑과 분리된 전용 물리 동작으로 옮긴다.**

| 요소 | 내용 |
|---|---|
| **누가 (대상)** | 로컬에서 Claude Code에 **파일 수정·쉘 명령 실행을 맡기되, 실행 직전 승인·거절은 직접 결정**하고 **그 승인을 타이핑과 분리하고 싶은** 개발자(human-in-the-loop). |
| **어떤 문제** | 작업 지시(코드·프롬프트)와 실행 승인이 **같은 키보드에 섞인다.** 승인이라는 별도 판단이 일반 타이핑과 **같은 입력 채널**을 쓴다. |
| **언제·얼마나 자주** | Claude Code가 **실행 승인을 요청할 때마다**(파일 수정·쉘 명령 등의 실행 직전). *지정 도구 호출마다 대기시키는 Hook PoC의 동작은 사용자 승인 빈도와 구분한다.* |
| **지금은 어떻게** | 터미널에서 요청 내용을 읽고 **키보드로 승인 메뉴를 선택·확정**한다. 다른 창에서 작업 중이었다면 승인 화면을 확인하고 **입력 포커스도 옮겨야** 한다. |

📌 **정직 경계(빈도·행동 과대 금지):**
- `CLAUDE.md`의 "Wait for Explicit Approval" 12곳은 **AI-DLC 단계 진행 승인 규칙 수**이지 *실행 승인 발생 횟수*가 아니다 → **빈도 근거로 쓰지 않는다.** 실제 일별·작업별 승인 횟수는 **미측정**.
- "무의식적 Enter 연타 → 오승인"은 **검증 안 된 사용자 행동 가설**이라 제출 확정 사실에서 **제외**한다.
- 따라서 'AI 전체 통제'·'자동 안전 복구'·'모든 위험 감지'·**'생산성 향상'·'오승인 감소'**는 **주장하지 않는다**(현재 구현/측정으로 증명 불가).

**→ 제출 README `## 문제`/`## 해결 방법`에 붙일 문장(현재형·통합 예정 시제 구분) = §0.6.  rubric 6항목 대비 근거·정직 가드·닫아야 할 갭 = §0.7.**

**제품명 정책:** 이번 제출물 = `Re:senne HUMAN GATE`. 포스터의 8키 컨트롤러 + 로터리 다이얼은 **`demo/arduino-simulator/`에 코드로 구현·커밋**했다(실제 Codex·Claude를 PTY에서 구동 + AI-DLC 5단계 물리 키 제어, WiFi HTTP 직결). **유일한 선 = 두 층위 안 섞기** — ① 코드=구현·커밋·소프트웨어 테스트(116개, 가짜 CLI), ② 실제 AI·실기기=사람 확인(자동 테스트 증명 아님). 다중 하네스 일반화 등 미래 확장 비전은 로드맵으로 크게 말한다. 자세한 포지셔닝 기준 = [`CONCEPT_FINAL.md` §0.5](./CONCEPT_FINAL.md).

---

## 0.6 제출 README 블록 (현재형·통합 예정 시제 구분)

> 제출 문서 템플릿의 `## 문제`·`## 해결 방법`에 붙일 문장. **지금 사실 = 현재형, 아직 통합 안 된 부분 = "통합 예정"**으로 시제를 구분한다(완성 시제 규칙 준수 — 비전은 미래형으로 당당히). 미측정 성과 주장 없음. README 작성 시 이 절을 복사한다.

### `## 문제`

Claude Code에 파일 수정과 쉘 명령 실행을 맡기면서, 실행 직전 승인·거절은 직접 결정하고 **그 승인을 타이핑과 분리하고 싶은** 개발자가 대상입니다.

**작업 지시(코드·프롬프트)와 실행 승인을 같은 키보드로 처리합니다.** 승인이 필요한 실행 요청이 나올 때마다 개발자는 터미널에서 내용을 읽고, 작업에 쓰던 키보드로 승인 메뉴를 선택·확정합니다 — 승인이라는 별도 판단이 일반 타이핑과 같은 입력 채널을 씁니다.

이 순간은 Claude Code가 실행 승인을 요청할 때마다(파일 수정·쉘 명령 등의 실행 직전) 반복됩니다. 사용자별 일일 발생 횟수는 아직 측정하지 않았습니다.

### `## 해결 방법`

**Re:senne HUMAN GATE는 실행 승인·거절을 전용 물리 버튼 두 개로 분리합니다.** 개발자는 화면에서 실행 요청을 확인하고 UNO R4 WiFi의 **✓ 승인(D2)** 또는 **✕ 거절(D3)**을 누릅니다. 제출 버전은 사람의 물리·웹 시뮬 입력을 **Python PTY 키응답**으로 실제 Claude Code의 대기 중 요청에 전달합니다 — **현재 이 저장소에서 바로 실행되는 완성 경로 = USB 시리얼 → PTY 키응답**입니다. **UNO R4 WiFi의 Node Bridge 경로(버튼→WebSocket→브리지→PTY)는 버튼↔브리지 왕복까지 실기기 확인**했고, **브리지↔Python 어댑터 통합이 남은 단계**입니다(§2).

**`hook_bridge.py`의 PreToolUse allow/deny 게이트 — 코드 통합·단위검증·소프트웨어 E2E 완료(실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크는 사람 몫).** 승인 브로커(`server.py`의 `/api/approval*` + `hooks/hook_bridge.py`)를 이 저장소에 코드로 통합하고 단위테스트 + 서브프로세스 E2E 테스트(`tests/test_approval.py`)로 검증했습니다. 진짜 `hook_bridge.py` 프로세스 ↔ 브로커 ↔ 브라우저(ui 토큰) resolver 체인을 서브프로세스 테스트 3개 + 실서버 curl 스모크로 소프트웨어 E2E 검증했고, 브라우저 승인 패널 **`/gate.html`에서 사람이 대기 중 도구 실행에 ✓ 허용 / ✕ 거부를 제출할 수 있습니다.** 팀 실기기 PoC에서 버튼→브리지 왕복까지 확인됐지만(Node Bridge↔Python 어댑터 연결은 통합 남은 단계), **실행 중인 진짜 `claude`가 그 결정을 존중해 실제 도구 실행을 allow/deny 하는지는 라이브 세션에서 확인하는 단계(사람 몫)**입니다. 게이트는 프로젝트 기본 설정이 아니라 **별도 예시 settings(`hooks/hook-gate.settings.example.json`)로 옵트인 활성화**합니다. **PTY 키응답 경로가 이 제출의 유일한 *완전* E2E 검증 경로**이며, 제출 저장소의 `hooks/claude_state_hook.py`는 권한을 결정하지 않는 상태 신호입니다. 또한 처리된(resolved) 게이트 결정은 **영속 감사 로그**(`server.py` `resolve_approval` → `.claude/approval-log.jsonl`, mode 0600·웹루트 밖·gitignore)에 `tool_name·input_hash·decision·role·시각`만 남고(summary·tool_input·토큰 제외), 브라우저 패널의 **최근 결정 뷰**(`GET /api/approval/log`, ui/device 토큰만)로 조회합니다. `role`은 사용한 자격증명 역할(ui/device)일 뿐 사람의 신원이나 실제 물리버튼 사용, 도구 실행 결과를 증명하지 않으며, 기록에 실패하면 결정을 전달하지 않아(요청 pending 유지) 훅은 fail-safe로 `ask`가 됩니다.

매크로패드·스트림덱은 보통 버튼에 단축키·명령을 매핑해 **실행을 촉발**하는 용도로 쓰입니다. HUMAN GATE는 버튼을 **이미 제안된 실행 요청의 승인·거절 결정**에 연결합니다.

설계 범위는 공통 프로토콜과 하네스별 어댑터를 통한 AI 하네스 연동이며, **현재 실기기 E2E로 검증된 예시는 Claude Code입니다.** 리센느 사진 키캡은 첫 커스텀 프로파일입니다.

---

## 0.7 AI 심사 rubric 6항목 정합 매트릭스

> 공식 rubric(100점): AI-DLC 25 / 문제정의 20 / 창의성 15 / 완성도 15 / 사용성 15 / 유지보수·보안 10. 각 항목을 **어디서 증명하고, 어디까지만 주장하고, 무엇을 닫아야 하는지**로 고정한다.

| 항목(배점) | 우리 근거 (어디서 증명) | 정직 가드 / 닫아야 할 갭 |
|---|---|---|
| **AI-DLC 활용도 (25)** | 이어지는 결정 사슬 1개(§5): **INTENT**(FR-3, 실제 Claude에 승인 응답 전달) → **ASK**(키 매핑을 설정표로 분리, Q2=C) → **PLAN**(설계 D2) → **BUILD**(`ACTION_KEYS`·`send_choice()`) → **VERIFY**(미등록 입력을 PTY 쓰기 전에 거절하는 속성 테스트). 앞 단계 결정이 뒤에 실제로 반영됨. | 이 사슬의 증명 범위 = **기존 입력 전달층.** 최종 Hook 게이트로 바꾼 결정은 `audit.md`에 **실제 변경 시점**으로 추가하고 요구사항·설계·build-and-test까지 이어붙인다. **기존 테스트도, 게이트 단위·서브프로세스 E2E 테스트(`tests/test_approval.py`)·브라우저 패널 테스트(`tests/gate.test.js`)도 소프트웨어 체인까지만 증명 — 실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크의 검증으로 대체 금지.** |
| **문제 정의 및 해결 (20)** | §0(대상·문제·언제·현행 표) + §0.6(README 블록). 구조적 차이 = "실행 촉발 → 이미 제안된 실행의 승인·거절". | 미측정 성과(오승인 감소·생산성) 문장 삭제(§0 정직 경계). 빈도는 "실행 승인 요청 시마다"까지만, 숫자 주장 금지. "전용 수단이 없다"(순환논리) 대신 "같은 키보드에 섞인다"로 서술. |
| **창의성 (15)** | 구조적 차별점(실행 촉발 → 승인·거절)이 **제출 repo 코드로** 증명됨: `server.py`(승인 브로커 — `create_approval` 중복제거 · `resolve_approval` 원자 CAS · `wait_for_decision` deadline→ask) · `hooks/hook_bridge.py`(실행 대기 후 `permissionDecision` allow/deny/ask 반환) · `tests/test_approval.py`(단위 + 서브프로세스 E2E) · 브라우저 승인 패널 `gate.html`·`gate.js`(`tests/gate.test.js`) · 기존 PTY 경로 `terminal.py`(`ACTION_KEYS`·`send_choice()`). AI-DLC 설계 문서 = `aidlc-docs/construction/approval-gate/design.md`. | 게이트는 이제 제출 repo에 **코드 통합 + 단위검증 + 소프트웨어 E2E 완료**(subprocess·curl 스모크·브라우저 패널)이지만 **실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크는 미검증(사람 몫)**. `terminal.py` 실행 경로·프로젝트 `.claude/settings.json`은 건드리지 않음(검증된 PTY 데모 회귀 방지·개발 세션 마비 방지 — astra 배선범위 A). **현재형은 "브라우저에서 게이트 요청에 allow/deny를 제출할 수 있다"까지만 허용 → "라이브 `claude` 결정 존중은 미검증·사람 몫"을 함께 명시.** PTY 키응답 경로가 제출 유일 *완전* E2E 검증 경로. |
| **완성도 (15)** | 무편집 B 영상 + `screenshots/`(또는 `result/`)에 **① 승인 대기 → ② 승인 후 실행 → ③ 거절 후 미실행** 3컷. 진입점→실제 구현 도달, 스샷↔README 정합. | 제출 폴더에 `screenshots/`·`result/` **현재 없음 → 생성 필수.** 핵심 경로 TODO/빈함수 제거(펌웨어 `request_id` echo TODO 포함, 아래). **오류 처리 검증**: 버튼 연결 끊김·브리지 종료 시 오류를 알리고 **의도치 않은 승인이 생기지 않는지** 확인·기록. |
| **사용성 (15)** | README 첫 실행 경로 = **연결 → Claude 시작 → 요청 내용 화면 확인 → ✓ 실행 / ✕ 미실행 확인.** OLED·화면 인터랙션 피드백(대기/실행/완료/오류/오프라인, §7). **승인 대기·결정 전달·실행 결과를 사용자가 어디서 확인하는지** README·시연에 명시. | "버튼 전송 성공"만으로는 핵심 시나리오 완주 아님 → **끝까지 통과하는 하나의 경로**를 README에 고정하고 그대로 따라가 막히지 않는지 확인. |
| **유지보수·보안 (10)** | 시크릿 = 환경변수(공개 repo 미포함), localhost 가드(Host allowlist·토큰 compare_digest·Origin·CSP·X-Frame-Options), 로컬 승인 API. 요청별 결합 = `request_id` 최초 1회. **게이트 결정 감사 로그**(RESILIENCY-05 준수 근거): 처리된 결정만 `.claude/approval-log.jsonl`(0600·웹루트 밖·gitignore)에 `tool_name·input_hash·decision·role·시각`만 기록(summary·tool_input·토큰 제외), 조회는 ui/device 토큰만(`GET /api/approval/log`), 기록 실패 시 결정 미전달(fail-safe ask). | `firmware/resenne_uno_r4/resenne_uno_r4.ino:162`의 **`request_id` echo TODO(통합)** 해결 + 오래된 입력이 새 요청에 적용되지 않음을 검증. **`request_id` 중복 처리(정합)와 송신자 인가(보안)를 구분** — 연결 가능한 주체·승인 입력 허용 조건을 제출 코드 기준으로 명시. 감사 로그의 `role`은 자격증명 역할일 뿐 신원·물리버튼·실행결과 증명이 아니며, 이 한 증분으로 RESILIENCY-05 완전 준수를 선언하지 않는다. |

---

## 1. MVP 범위 (반드시 / 가능하면 / 버릴 것)

| 구분 | 항목 |
|---|---|
| **반드시** | UNO 물리 버튼 → 기존 Python PTY 승인 처리 → **실제 Claude 실행** |
| **반드시** | 브라우저에 현재 작업 / 승인 대상 / 승인 대기 / 승인 결과 표시 |
| **반드시** | OLED에 연결상태·단계·승인대기 표시 (§7 카피표) |
| **반드시** | 승인 **대기 중인 요청에만** 버튼 적용 · 중복 입력 방지 · 연결 끊김 표시 |
| **반드시** | 실제 동작 영상 · `screenshots/` · README · `aidlc-docs/` |
| **반드시** | **첫 실행 검증(사용성):** 처음 보는 검증자가 README 사전조건(지원 환경·UNO 펌웨어·Claude Code 설치·필요한 계정/인증·환경변수)·설치/실행 명령·데모 예제·승인/거절 조작·결과 확인·트러블슈팅 순서만 따라 **핵심 시나리오를 끝까지 완주**한다 |
| 가능하면 | 내장 12×8 LED 매트릭스: 대기(느린 점멸)·실행(이동)·완료(체크) |
| **반드시** | **두 번째 물리 버튼(✕ REJECT/STOP) = 현재 요청 거절** — 실물 2개 제작 완료, D3 배선 + 기존 NO 경로 재사용 |
| **버릴 것** | 8키 제작 · 로터리 다이얼 · 자율성 5단계 · 새 케이스 설계 |
| **버릴 것** | 범용 승인 프롬프트 해석기 · 자동 롤백 · 새 에이전트 엔진 |

**결정 사항:**
- **물리 버튼 2개 제작 완료** → 버튼1 = ✓ APPROVE(D2), 버튼2 = ✕ REJECT/STOP(D3). 한 버튼에 짧게/길게 조합은 금지(2개로 분리했으니 불필요). **시연의 주인공(머니샷)은 버튼1 APPROVE로 유지**하고, 버튼2 거절은 안정적일 때만 보조 비트로 넣는다.
- 포스터의 `STOP / REVERT`는 **구현 기능처럼 제출하지 않는다**(중단과 복구는 별개, 지금의 핵심도 아님).
- 실물 경로의 **자동 APPROVE는 반드시 끈다** — 사람이 안 눌러도 진행되면 컨셉 자체가 무너진다.

---

## 2. 아키텍처 통합 (교체 아님 — 얇은 연결)

**A(실물 연결) + B(실제 실행·승인 상태)를 재작성 없이 어댑터로 잇는다.**

경로 B(물리 버튼 → 브리지 → 기존 Python PTY 승인 → 실제 Claude 실행)는 **버튼→브리지 왕복까지 실기기 확인**했고, Node Bridge↔Python 어댑터 연결은 통합 남은 단계다. 아래 연결도는 **제출본 통합 목표**이며, 신규 로컬 어댑터의 상태·입력·결과 연동을 제출본에서 검증한다. **현재 제출 저장소에서 바로 실행되는 승인 경로 = USB 시리얼 → PTY 키응답**이고, **UNO R4 WiFi의 Node Bridge↔Python 어댑터는 버튼↔브리지 왕복까지 실기기 확인·나머지는 통합 남은 작업**이다. PreToolUse allow/deny 게이트는 이제 제출 repo에 **코드 통합·단위검증·소프트웨어 E2E 완료**(브로커·서브프로세스 E2E + 브라우저 패널 `/gate.html` + curl 스모크, 별도 예시 settings로 옵트인)이나 **실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크는 미검증** → 이번 제출의 **완전 E2E 동작 근거는 여전히 PTY 키응답 경로**이며 게이트는 "코드 통합·단위검증·소프트웨어 E2E / 라이브 `claude` 존중은 사람 몫" 시제로 서술한다.

```text
UNO R4 WiFi + 버튼 + OLED
        ↕  기존 WebSocket JSON
기존 Node Bridge (:8080)
        ↕  작은 로컬 어댑터  ← 이번에 추가하는 유일한 신규 코드(얇게)
기존 Python 서버 → PTY → 실제 Claude Code
        ↕
Button Lab 브라우저 (sim.html / app)
```

**연결 3개만:**
1. **Python → Node (승인 상태 전달):** Python의 승인 대기 판정 결과를 Node가 받아 UNO에 `state_update` 방송. 스트리밍이 부담이면 데모에선 Node가 작은 상태 API를 **~200ms 폴링**으로 충분.
2. **Node → Python (물리 입력 전달):** UNO의 `APPROVE`(버튼1)·`REJECT`(버튼2)를 Node가 Python 기존 버튼 처리 API(`/api/press` — `confirm`/`no`)로 전달. **PTY 입력 코드는 새로 안 만든다.** 두 버튼 모두 대기 요청 있을 때만·request_id 최초 1회만 처리.
3. **Python → 화면·기기 (실제 결과 전달):** 승인 요청을 받아들였다는 이유만으로 `SUCCESS` 금지. **실제 세션 진행 확인 → `RUNNING`, 데모 작업 검증 통과 → `SUCCESS`**.

**구현 4조건(안전·정합):**
- **진실의 기준 = Python.** `sim.html`의 타이머가 실제 상태를 결정하면 안 된다.
- 승인 요청에 `request_id`를 붙이고 버튼 응답에 되돌려받아, **현재 대기 요청과 일치하는 최초 1회만** 처리.
- 기기 재연결 시 남아 있던 입력 **재전송 금지**.
- 승인 API는 **로컬 연결로 제한**, 공유 키는 **환경변수**로만(공개 저장소에 절대 미포함). 기존 localhost 가드(Host allowlist·X-Simulator-Token compare_digest·Origin·CSP·X-Frame-Options: DENY) 유지. **UNO용 WebSocket은 필요한 인터페이스만 노출하고 환경변수로 주입한 연결 토큰으로 인증**하며, 입력은 **허용된 action과 현재 `request_id`만 통과**시키고 Python이 **대기 여부 확인 + 최초 결정 소비를 원자적으로** 처리한다.

**완성도·유지보수 계약(rubric 대비):**
- **빌드·실행(완성도):** 단일 진입점 `python3 server.py`(기본 `127.0.0.1:8765`) · Node 의존성 선언 `package.json`+`package-lock.json` · 런타임 무의존(프런트는 `vendor/`) — **이미 저장소에 존재.** README 설치·실행 명령을 **깨끗한 환경에서 재현 검증**하고, 여유가 되면 구문 검사·핵심 상태 전이·테스트 실행을 **CI(또는 문서화된 단일 명령)**로 남긴다. 물리 버튼 E2E는 별도 실기기 실행 기록으로 증명한다.
- **에러 핸들링(완성도):** 연결 실패·타임아웃·PTY 종료·검증 실패는 주요 경로에서 처리하고, Node/Python **전역 예외 처리기**가 오류 기록·입력 차단·정리를 수행한다. 화면엔 `ERROR`/`OFFLINE`와 복구법을 표시하고, **복구 후 기존 승인을 자동 재전송하지 않는다**(상태 리더는 예외를 던지지 않고 `offline`로 수렴 — 기존 구현).
- **구조화 로그(유지보수·관측):** Python(진실 기준)이 `request_id`·시각·상태 전이·입력 출처·승인/거절·중복/비대기 입력 차단 사유·연결 변화·실행/검증 결과·오류를 **구조화 로그**로 남기고, 화면 실행 기록과 **같은 `request_id`로 대조**한다. 인증정보·프롬프트·코드 원문은 기록하지 않는다.

---

## 3. 데모 시나리오 (45~55초 · 주인공 = 누르기 직전 3초)

**데모 작업 고정:** 간단한 행사 안내 웹페이지의 **신청 버튼 문구·색상 변경** (전후 차이 큼, 실패해도 복구 쉬움). 변경은 격리 폴더(`demo_target/`)에서만, 성공 판정용 검증(sentinel/`verify`) 포함.

**⚠️ 사전 필수 검증:** 이 변경을 수행하는 **실제 Claude 명령이 현재 세션에서 승인 프롬프트를 띄우는지** 미리 확인. 이미 허용된 명령이라 승인 없이 실행되면 → 데모 작업이나 권한 설정을 바꾼다. **자연히 생기지 않는 승인 대기를 화면 연출로 위조하지 않는다.**

| 시간 | 화면·행동 | 멘트 |
|---|---|---|
| 0~7초 | 기존 웹페이지 + 실제 Claude 터미널 함께 표시 | "AI에게 신청 버튼을 바꿔 달라고 했습니다." |
| 7~17초 | Claude 실제 승인 대기. 웹페이지는 그대로 | "실행 준비는 끝났지만, 아직 바뀌지 않습니다." |
| 17~27초 | 카메라가 **리센느 사진 키캡 + OLED** 클로즈업 | "마지막 승인은 사람 손에 있습니다." |
| 27~30초 | 손가락을 버튼 위에 두고 **3초 침묵** | (정지) |
| 30~40초 | **클릭 → Claude 진행 → 변경 반영** | "리센느를 누르면, 실제 Claude가 실행합니다." |
| 40~50초 | 바뀐 페이지 + 완료 OLED를 같은 구도로 | **"사람이 방향을. AI가 속도를. 실행 승인은, 이 손에."** |

핵심 증거 = **누르기 전엔 그대로였고, 누른 뒤 실제로 달라졌다는 인과관계.** 효과음·애니메이션보다 이걸 우선. 클릭→실행 시작은 가능한 한 **끊지 않은 한 화면**으로 촬영.
- Claude 작업이 길면 자연어 요청부터 라이브로 시작하지 말고 **"실제 승인 대기까지 준비된 세션"에서 시작**하고 영상에 그렇게 표시.
- **물리 거절(✕)은 MVP 필수 검증·기록:** 새 요청에서 ✕ 버튼 → 실제 거절 → **해당 요청 미실행**을 확인하고 `screenshots/`에 남긴다(§10). **영상 삽입(+10초)만 선택** — 버튼2가 3회 연속 안정 동작할 때만 넣고, 불안하면 영상에선 빼되 **거절 실행 기록·스샷은 유지**(히어로 컷은 APPROVE). 거절 표시는 §7과 동일 라벨(OLED `DECLINED` / 터미널 "Permission denied by human gate").

---

## 4. 갤러리 미디어 전략

**썸네일 1장 + 무음으로 이해되는 25초 영상 1개 + 증거 스크린샷 2장**이면 충분.

**썸네일**
- 왼쪽: 손가락 + 리센느 사진 키캡 + OLED `YOUR TURN`
- 오른쪽: 실제 Claude 승인 대기 화면(읽히게)
- 큰 카피: **"실행 앞에, 사람."** *(Before it runs, a human.)* / 작은 카피: `물리 버튼으로 AI 하네스의 대기 중 실행을 승인·거절 — 검증된 예시 = Claude Code`
- **포스터는 첫 이미지로 쓰지 않는다**(없는 8키 제품 기대를 만듦). 뒤쪽에 `DOCKPAD 확장 콘셉트`로 표시.

**25초 영상 컷**
- 0~4초 `실제 Claude Code · 실행 승인 대기` / 4~10초 OLED·사진 키캡 클로즈업 / 10~13초 손가락 대기→클릭 / 13~21초 터미널 진행+웹페이지 변경 / 21~25초 **`버튼의 재정의. 명령에서 승인·거절로.`** *(Redefining the button. From commands to consent.)* + 리센느

**스크린샷 2장:** ① 승인 전후 + 실행 기록 ② AI-DLC 문서에서 실제 변경·검증으로 이어지는 근거.

**제출 문구(그대로 사용 가능):**
> **제목:** 리센느 HUMAN GATE — 명령에서 승인·거절로
> **설명:** Claude Code의 실행 승인권을 책상 위로 꺼냈습니다. AI가 준비하면 OLED가 사람을 부르고, 리센느 버튼을 누르면 실제 코드 변경이 진행됩니다. AI-DLC로 만들고, 사람의 승인으로 실행하는 실물 Human-in-the-loop 컨트롤러입니다.

---

## 5. AI-DLC 활용도 25점 (파일 개수 아님 — "결정의 증거")

`aidlc-docs/`는 기존 문서를 유지하고, **최상단 안내 문서 한 페이지**에 아래 추적을 만든다:

| 단계 | 이번 프로젝트에서 보여줄 실제 근거 |
|---|---|
| INTENT | "실행 승인 순간을 물리 입력으로 분리한다"는 문제와 성공 조건 |
| CONTEXT | UNO 실측 제약, 검증된 A/B 경로, 재사용 결정 |
| ASK | 한 버튼의 역할, 승인 상태의 기준, 통신 끊김 처리 질문·결정 |
| PLAN | A/B 연결 설계와 사람이 승인한 구현 범위 |
| BUILD | 연결 어댑터와 실제 코드 변경 링크 |
| VERIFY | 기존 테스트 + 게이트 단위·서브프로세스 E2E(`tests/test_approval.py`)·브라우저 패널(`tests/gate.test.js`) + curl 스모크(소프트웨어 E2E) + **라이브 `claude` 결정 존중은 사람 몫** |
| REVIEW | 요청 중복·연결 장애·노출 범위 검토와 남은 제한 |

- 심사자가 보는 건 **한 줄 추적**: `요구사항 → 사람의 범위 승인 → 구현 변경 → 검증 결과 → 리뷰`.
- **가장 확실히 이어지는 결정 사슬 1개(구체·실재):**
  > **INTENT** 실제 Claude 세션에 승인 응답 전달(FR-3, [requirements.md:38]) → **ASK** 키 매핑을 설정표로 분리(Q2=C) → **PLAN** 설계 D2([application-design.md:12]) → **BUILD** `ACTION_KEYS`·`send_choice()` → **VERIFY** 미등록 입력은 PTY 쓰기 전에 거절하는 속성 테스트([tests/test_mapping_pbt.py:39]).
  - 이 사슬의 **증명 범위 = 기존 입력 전달층**(키 응답 경로). 정직하게 그 범위로만 제시한다.
  - **최종 Hook allow/deny 게이트로 바꾼 결정**은 `audit.md`에 실제 변경 시점으로 추가하고, 요구사항·설계·`build-and-test-summary.md`까지 이어붙인다. VERIFY에는 **팀 실기기 검증·사용 코드 버전·승인 시 실행/거절 시 미실행 증거·AI의 실기기 재검증 미수행**을 명시.
- **기존 테스트 통과도, 게이트 단위·서브프로세스 E2E 테스트도 라이브 `claude` 결정 존중의 검증인 척 쓰지 않는다.** 게이트 신규 경로의 **정상 승인 / 중복 승인(dedup) / 대기 없는 입력(unknown→ask) / 세션 종료 만료 / 토큰 3역할**은 `tests/test_approval.py`로 단위검증했고, 진짜 `hook_bridge.py` 프로세스 ↔ 브로커 ↔ 브라우저(ui 토큰) resolver 체인은 서브프로세스 E2E + curl 스모크로 소프트웨어 검증했다(브라우저 패널 `/gate.html`). **실행 중인 진짜 `claude`가 그 결정을 존중하는 라이브 링크는 사람 몫.**
- **과거 승인 기록을 소급 조작 금지.** 지금부터 실제 결정을 기록.
- 명확히 구분: **이번 MVP가 직접 제어하는 것 = Claude의 실행 승인.** AI-DLC 전 과정 자동 제어는 확장 방향(개발 과정에 AI-DLC를 썼다는 증거는 충분히 제출).

**문서 정합성 정리(감점 방지, 필수):** 현재 저장소 README/일부 aidlc-docs가 최종 HW를 **BindDeck ESP32**로, 테스트를 **43 passed**로 기록 → 실제 제출은 **UNO R4 WiFi / 77 tests(54 Python + 23 JS)**. README·`aidlc-state.md`·requirements/design의 "최종 구현 경로"를 UNO로 통일하고 BindDeck은 "과거 실험 경로"로 격리, 테스트 수치를 **77(54 Python + 23 JS)로 통일**.

---

## 6. 네이밍·카피 (3개만)

- **제품명:** `Re:senne HUMAN GATE`
- **메인 카피:** "버튼의 재정의. 명령에서 승인·거절로." *(Redefining the button. From commands to consent.)*
- **갤러리 훅(썸네일):** "실행 앞에, 사람."
- **발표 오프닝:** "오늘 데뷔하는 Re:senne HUMAN GATE는, AI 하네스의 실행 요청에 사람이 두 버튼으로 답하는 새로운 역할을 선보입니다 — 그 첫 실증이 Claude Code입니다."
- 카피 권위본 = **[`CONCEPT_FINAL.md` §0.5](./CONCEPT_FINAL.md)**. 옛 카피("AI가 기다리는 버튼"·"코딩은 Claude가…"·"AI keeps coding…")는 쓰지 않는다. `DOCKPAD`는 미래 제품명으로 보존하고, **"리센느 = AI 승인·거절 게이트" 기억 하나**를 만든다.

---

## 7. OLED + 사진 키캡 연출

OLED = 설명 화면이 아니라 **"지금 누구 차례인지"** 화면. 작은 화면엔 긴 한글 대신 **큰 영문 두 줄 + 짧은 상태**.

| 상태 | 큰 문구 | 작은 문구 |
|---|---|---|
| IDLE | `READY` | `Re:senne` |
| RUNNING | `AI'S TURN` | `BUILD` |
| **REVIEW_REQUIRED** | **`YOUR TURN`** | `PRESS TO APPROVE` |
| SUCCESS | `DONE` | `CHECK PASSED` |
| **REJECTED** | **`DECLINED`** | `NOT EXECUTED` |
| ERROR | `CHECK SCREEN` | `ACTION NEEDED` |
| DISCONNECTED | `OFFLINE` | `CHECK LINK` |

- `DONE / CHECK PASSED`는 **지정 검증이 실제 통과했을 때만** 표시. 승인할 명령·대상 파일은 큰 브라우저 화면에서 확인.
- **전송 피드백과 상태 확정 분리(정합·인터랙션 피드백):** 승인·거절을 누른 **직후에는 "전송됨" 피드백만** 보이고, **실제 상태를 확인하기 전에는 `DONE`/`AI'S TURN`/`DECLINED`로 확정하지 않는다.** 거절 라벨 `DECLINED`는 **펌웨어·README·영상에서 동일하게** 쓴다 — **현재 펌웨어/README OLED 표엔 거절 라벨이 없으므로 추가·통일이 필요**하다(정합 작업, §10).
- SSD1306 기본 폰트는 한글·✓/✕가 깨질 수 있으니 **영문 대문자**. 체크 표시가 필요하면 텍스트 대신 작은 bitmap.
- 키캡 사진은 가리지 않고 옆에 작은 라벨. **버튼 2개 구분 필수:** 버튼1 = **`실행 승인`**(초록/세이지 톤 LED), 버튼2 = **`실행 거절`**(앰버 LED). 두 캡 모두 리센느 사진이면 **위치 + 라벨 + LED 색**으로 의미를 구분한다. OLED와 키캡을 붙여 카메라 한 컷에.
- 내장 LED 매트릭스: 대기=느린 점멸, 실행=이동, 완료=체크 정도만.
- 키캡은 손 올리기 전 1초 보여주고, 옆에서 눌러 **사진과 눌림을 함께** 촬영. 클릭 소리도 담기.
- **키캡 위트:** "Human in the loop. 진짜 사람 얼굴까지 넣었습니다." → 한 번 웃기고 바로 실제 승인 동작으로.
- 다이얼은 실물이 없으므로 가짜 다이얼을 UI에 크게 넣지 말 것. 필요하면 화면 한쪽에 작게 `AUTONOMY: 0 / MANUAL`.

---

## 8. 리스크 → 대비

| 리스크 | 대비 |
|---|---|
| 제출본 통합 미완료(브리지↔Python) | **완전 E2E 검증 경로(PTY 키응답)를 제출 repo 시작 명령으로 재현.** 고정 세션·데모 작업에서 실제 프롬프트 확인, `APPROVE=Enter`/`REJECT=Esc` 실제 효과 각 3회 + 거절 후 해당 실행 미진행 확인 |
| Claude가 승인 없이 진행 / 예상과 다른 메뉴 | 데모용 세션·작업·프롬프트 고정, 매 시연 전 실제 대기 확인, **자동 APPROVE OFF 재확인** |
| 주요 경로 오류(연결 실패·타임아웃·PTY 종료·검증 실패) | 각 경로에서 처리 + Node/Python **전역 예외 처리기**가 오류 기록·입력 차단·정리. 화면에 `ERROR`/`OFFLINE`와 복구법 표시, **복구 후 기존 승인 자동 재전송 금지** |
| WiFi·WSL 주소 문제 | 전용 2.4GHz 준비, IP·portproxy 사전 확인, 시작 스크립트로 재설정 + `/health` 체크 |
| 중복 클릭·지연 입력 | request_id + 최초 1회 처리, 대기 상태 외 입력 무시 |
| OLED 추가로 안정성 하락 | 통신 루프 막지 않게 상태 변경 위주 갱신, 문제 시 내장 매트릭스로 축소 |
| 라이브 실패 | **실물→실제 Claude 성공 영상 미리 확보**, 재생 영상임을 표시하고 설명 지속 |
| 시크릿 노출 | 실제 값은 **환경변수로 주입·저장소 제외**, 설정 예시는 **빈 값(placeholder)** 제공, **로그·스크린샷·영상의 노출 여부 확인** |

---

## 9. 시간표 (고정)

| 블록 | 목표 |
|---|---|
| **첫 2시간** | 제출 repo의 **의존성·설정·시작 진입점으로 경로 B 재현** → 실제 승인 후 실행·검증 성공 + 실제 거절 후 해당 실행 차단을 **각 3회 확인**, `request_id` 연결 로그 보존. 실패하면 시각 작업 멈추고 연결에 올인 |
| 다음 3시간 | 요청 중복 방지 · 상태 동기화 · OLED · 데모 작업 고정 |
| 다음 2시간 | 갤러리용 화면 정리 · 통합 확인 · 문서 근거 연결 |
| **9/9 14:00** | 기능 동결 |
| 14:00~15:00 | 실제 성공 영상 + 썸네일 촬영 |
| 15:00~16:00 | 동결본 기준 **README 첫 실행 재현** · 소스 · `aidlc-docs/` · `screenshots/`(또는 `result/`) 시연 스샷 · 영상의 **정합 확인** 후 제출 |
| 16:00 이후 | 제출 화면 점검 + 시연 리허설 |

> **첫 2시간의 성공 조건은 딱 하나:** 리센느 사진 키캡을 눌렀더니, 기다리던 실제 Claude가 실행된다. 이 장면을 확보한 뒤 남은 시간은 전부 "쉽게 보여주기"에 쓴다.

---

## 10. 제출 전 완료 기준 (Definition of Done)

- [ ] **★ 최우선(정합):** 제출 버전의 **완전 E2E 동작 근거 경로 = PTY 키응답(USB 시리얼→PTY)**으로 확정하고 README 메커니즘 설명·코드 위치·팀 실기기 E2E·시연·`screenshots/`를 **같은 버전에 맞춘다.** Hook allow/deny 게이트는 이제 **코드 통합·단위검증·소프트웨어 E2E 완료(`tests/test_approval.py` 서브프로세스 E2E + 브라우저 패널 `/gate.html`·`tests/gate.test.js` + curl 스모크)**이나 **실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크는 미검증(사람 몫)** → "코드 통합·단위검증·소프트웨어 E2E 완료 / 브라우저에서 allow/deny 제출 가능 / 라이브 `claude` 존중은 사람 몫 / 별도 예시 settings로 옵트인"으로 기술(현재형 "실행 중인 claude를 게이트가 막는다" 금지). — §0.6·§0.7과 일관
- [ ] 실물 UNO ✓ 입력 1회가 **실제 Claude PTY 승인 프롬프트**를 통과시킨다
- [ ] `REVIEW_REQUIRED → RUNNING → SUCCESS`가 OLED와 화면에 동시에 보인다
- [ ] 거절(✕ 물리 버튼2) 또는 중단이 실제로 동작한다
- [ ] 같은 시나리오를 **연속 3회 재현**한다
- [ ] 하드웨어가 끊겨도 **mock/browser fallback**으로 데모 가능
- [ ] README · `aidlc-docs/` · 테스트 수치(77 = 54 Python + 23 JS) · 영상이 **UNO R4 WiFi 기준으로 일치**
- [ ] 공개 저장소에 비밀정보 0 · 승인 API 로컬 제한 · localhost 가드 유지
- [ ] **(완성도)** `screenshots/`(또는 `result/`)에 **승인 대기 / 승인 후 실행 / 거절 후 미실행** 3컷이 있고 README 기능과 일치
- [x] **(창의성)** B-검증 PoC(`hook_bridge.py` allow/deny 게이트)를 제출 repo로 **코드 통합 + 단위검증(`tests/test_approval.py`) + 소프트웨어 E2E 완료** — `server.py`의 승인 브로커(`/api/approval*`)와 `hooks/hook_bridge.py`가 진입점→게이트까지 실제로 닿고, 진짜 `hook_bridge.py` 프로세스 ↔ 브로커 ↔ 브라우저(ui 토큰) resolver 체인을 서브프로세스 E2E 3개 + 실서버 curl 스모크로 검증, 브라우저 승인 패널 `/gate.html`(`tests/gate.test.js`) 추가. 설계 문서 = `aidlc-docs/construction/approval-gate/design.md`. **실행 중인 진짜 `claude`가 결정을 존중하는 라이브 링크는 미검증(사람 몫)**이므로 "제출 repo가 실행 중인 claude의 실제 도구 실행을 게이트한다"는 현재형 주장 금지 — 옵트인(`hooks/hook-gate.settings.example.json`)이며 `terminal.py`·프로젝트 `.claude/settings.json`은 불변
- [ ] **(사용성)** README에 첫 실행 경로(연결→Claude 시작→요청 확인→✓/✕) 하나가 있고, 그대로 따라가 막히지 않음
- [ ] **(보안·유지보수)** 펌웨어 `request_id` echo TODO(`resenne_uno_r4.ino:162`) 해결 + 오래된 입력이 새 요청에 적용되지 않음을 검증
- [x] **(AI-DLC)** §5의 결정 사슬이 문서에서 실제로 이어지고, 게이트 코드 통합 결정이 `audit.md` + `aidlc-docs/construction/approval-gate/design.md`에 기록됨(기존 테스트도 게이트 단위·서브프로세스 E2E 테스트도 라이브 `claude` 결정 존중의 검증으로 대체하지 않음)

---

*작성 근거: gpt-6-astra(fast) 최종 기획 + gpt-5.6-luna 보완안 + 팀 정정(키캡=리센느 사진, 실기기=UNO R4 WiFi, 테스트 baseline=45 → 게이트 통합 후 68 → 결정 감사 로그 증분 후 77 = 54 Python + 23 JS). 상세 원문은 `/home/hj/.claude/jobs/` 세션 산출물 참조.*
